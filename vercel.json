## vercel 에서 사용하는 경우 vercel.json deploy 하기 위해서 필요함 , render에서는 사용 안해도됨
{
  "version": 2,
  "builds": [
    { "src": "app.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/.*", "dest": "app.py" }
  ]
}
